<h1>Editing Question</h1>

<%= render 'form', question: @question, method: 'put', target: "/questions/#{@question[:_uid_]}" %>

<%= link_to 'Back to question', controller: 'questions', action: 'show', id: @question[:_uid_] %>
